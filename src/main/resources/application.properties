spring.application.name=demo
spring.security.user.name=admin
spring.security.user.password=$2b$12$JiN9m73Ce./WX/QnfvgTduHMdR9hPCscb9sh/Jbf15SOEPVkpXOnO

# Conexión a PostgreSQL
spring.datasource.hikari.connection-timeout=30000
spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/mydatabase
#spring.datasource.url=jdbc:postgresql://localhost:5432/mydatabase
spring.datasource.username=myuser
spring.datasource.password=secret
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.address=0.0.0.0

# Clave secreta para firmar el JWT (mínimo 32 caracteres para HS512)
jwt.secret=clave-secreta-super-segura-de-32-caracteres-minimo!!

# Expiración del token en milisegundos (ejemplo: 1 día)
jwt.expiration=86400000

server.port=8443
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=mipass
server.ssl.key-store-type=PKCS12

spring.web.resources.static-locations=classpath:/static/,file:uploads/


# Ruta donde se guardan las imágenes
app.upload.dir=public/uploads/photos

# URL base para acceder a las imágenes
app.upload.base-url=http://localhost:8080/imagenes/